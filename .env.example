# Copy to .env.local for local overrides.
# Environment label shown in the topbar badge and used to gate simulations (environment.store.ts).
# One of: DEV, STAGING, RED_TEAM, BLUE_TEAM
VITE_SECUREBANK_ENV=DEV
